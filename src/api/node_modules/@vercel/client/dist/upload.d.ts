import { DeploymentFile } from './utils/hashes';
import { NowConfig, NowClientOptions, DeploymentOptions } from './types';
export declare function upload(files: Map<string, DeploymentFile>, nowConfig: NowConfig, clientOptions: NowClientOptions, deploymentOptions: DeploymentOptions): AsyncIterableIterator<any>;
