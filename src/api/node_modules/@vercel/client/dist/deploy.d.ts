import { DeploymentFile } from './utils/hashes';
import { DeploymentOptions, NowConfig, NowClientOptions } from './types';
export declare function deploy(files: Map<string, DeploymentFile>, nowConfig: NowConfig, clientOptions: NowClientOptions, deploymentOptions: DeploymentOptions): AsyncIterableIterator<{
    type: string;
    payload: any;
}>;
