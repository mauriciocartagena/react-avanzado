import { NowConfig, NowClientOptions, DeploymentOptions } from './types';
export default function buildCreateDeployment(version: number): (clientOptions: NowClientOptions, deploymentOptions?: DeploymentOptions, nowConfig?: NowConfig) => AsyncIterableIterator<{
    type: "warning" | "error" | "hashes-calculated" | "file-count" | "file-uploaded" | "all-files-uploaded" | "created" | "building" | "ready" | "alias-assigned" | "notice" | "tip" | "canceled";
    payload: any;
}>;
